---
# Experiment/Sample template. Please replace <key> with the metadata keys defined in the schema and <value> with its value
#
# Necessary info
dataset_name: <value>
experiment_id: # Multiple samples can be associated with a dataset. If this is the case, then the identifier NEEDS to be unique in all samples
  - # If a rigorous directory structure is used then this can be inferred from the directory and only needs explicit definition if multiple samples are associated with the dataset
  - <one of the identifiers associated with the experiment/sample that this dataset is part of>
  - <one of the identifiers associated with the experiment/sample that this dataset is part of>
dataset_ids: 
  - <a list of identifiers that are used for this object>
  - <a list of identifiers that are used for this object>
instrument_id: <one of the identifiers associated with the instrument (or commercial entity if outsourced) used to generate the dataset>
# schema: <value> - un-comment if multiple schema have been defined and set the appropriate type of schema
#
# Access control. Access, unless specified otherwise, cascades from the experiments associated with the dataset.
# If different access controls are needed, they need to be explicitly defined.
# To do so, uncomment the relevant section and fill it out.
# Groups are preferred over individual users for the purposes of admin
#
#admin_groups:
#  - <value>
#  - <value> # Add new rows prepended with - as needed, or remove as needed
#admin_users:
#  - <value>
#  - <value>
#read_groups:
#  - <value>
#  - <value>
#read_users:
#  - <value>
#  - <value>
#download_groups:
#  - <value>
#  - <value>
#download_users:
#  - <value>
#  - <value>
#sensitive_groups:
#  - <value>
#  - <value>
#sensitive_users:
#  - <value>
#  - <value>
#
# General metadata
#
metadata:
  <key>: <value>
  #
